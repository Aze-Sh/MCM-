# 赛前准备实施计划

> 执行方式：在当前已有工作目录中按任务推进；文档更新可委派，检查器与环境集成由主任务处理。

**目标：** 保留已有修改，建立明确入口，依据 2026 官网规则更新 AI 披露及检查器，实测 Python/MATLAB/XeLaTeX，并初始化 `国赛/contest/2026`。

**设计依据：** 用户于 2026-09-10 确认的赛前准备请求及此前仓库检查。

**约束：** 不重置、不清理、不合并或提交原有修改；先备份。正式目录不预选 A/B/C 题。环境隔离在仓库 `.venv`，避免改动其他项目的 Python。测试报告明确区分实测、历史记录、失败、跳过及空白模板。

## 任务与验证

- [x] 保存 15 个已有修改文件、SHA-256 清单和二进制补丁到外层 `tmp/preflight-backup-20260910-163747`。
- [ ] 更新 `rules/`、来源记录、论文 AI 声明和详情模板、工作流、Skill 控制说明，使用新的 `OFF-CUMCM-2026-AI-RULES` 来源编号，保留旧版记录为历史。
- [ ] 针对 AI 声明缺失、位置错误、未使用与已使用冲突、正文用途、详情 PDF 缺失/损坏/内容缺项、纯润色例外编写真实输入测试，先失败再实现。支持 `--stage draft|submission`，提交模式必须读取最终论文 PDF。
- [ ] 使初始化器复制可编译论文和图形模板，保留任何已有文件；检查器识别项目中的 `paper/`、`python/`、`matlab/` 布局。
- [ ] 创建本地 Python 环境并安装依赖；运行核心及完整测试；实际启动 MATLAB、运行 13 项方法测试及跨语言比对；实际编译论文和 AI 详情 PDF 并渲染查看。
- [ ] 重建并校验 Skill 资源包，外层添加 README 与项目指引、PowerShell 运行入口；初始化正式目录，保留原始数据目录和可复现实验入口。
- [ ] 独立审阅变更，修复重要问题，写入赛前报告与剩余人工事项。空白比赛项目的提交检查应失败，不能宣称可提交。

## 记录

- Ruling: 用户已明确授权上一轮提出的完整赛前准备范围，直接实施；不再请求相同范围的批准。
- Ruling: 现有目录已经是 `codex/cumcm-build` 工作树，原地修整以保留 15 项未提交修改；外层仅增加入口和比赛文件。
- Ruling: 各任务共享界面为新来源编号、模板字段和 `draft/submission` 阶段；由主任务统一集成与测试。
