# 第二套运行结果

`run_robot.py` 的默认输出位置为本目录下的 `runs/`，每次运行创建一个包含运行类型、题号和时间戳的子目录，保存 `actions.jsonl`、`metadata.json` 与 `summary.json`。显式传入 `--output` 时使用指定目录。

`runs/` 是本地工作区，默认不进入 Git。需要纳入论文证据的完整记录另行整理到本目录下按日期、题号和版本命名的目录，例如 `2026-09-12_q3_v7_rehearsal/`，保留官方原始日志文件名，并说明它属于合成测试、演练还是正式测试。

目前上传内容不含第二套的完整实机运行记录，本次整理未补写成绩。
