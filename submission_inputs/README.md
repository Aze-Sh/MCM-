# 待填提交材料

空目录用`.gitkeep`保留，里面没有虚构日志。当前已有的截图成绩只放在`practice_results.csv`，它们不是正式成绩。

| 位置 | 放什么 | 用途 |
|---|---|---|
| `formal/q3/test_1/`至`test_3/` | 问题三各次正式测试导出的原始`.jlog`，每目录一份 | 题目要求的三次正式记录 |
| `formal/q4/test_1/`至`test_3/` | 问题四各次正式测试原始`.jlog`，每目录一份 | 题目要求的三次正式记录 |
| `formal_results.csv` | 六次案例编码、实际清除数、总虚拟时间、平均时间、现实时间、原始文件名、上传是否成功 | 自动生成正文第28页两张正式成绩表 |
| `practice_results.csv`、`practice_details/` | 十次演练的清除数、现实时间和匿名核对材料 | 补充正文演练结果；不要把截图源数直接当清除数 |
| `q2_comparison/` | 同输入下问题二方案比较的配置及真实结果 | 补正文问题二的实测对照空栏 |
| `sensitivity/` | 参数实验的输入、参数、结果和必要匿名记录 | 补参数敏感性表 |
| `ablation/` | 同输入模块消融的配置、结果和必要匿名记录 | 补消融表 |
| `human_review.json` | 实际完成的核查事项设为true，并填匿名备注 | 提醒人工核查；不能代替AI使用详情中的实际说明 |

时间统一用秒；平均时间=总虚拟时间/实际清除数。0个清除时平均时间留空，并在论文说明结束原因。正式模块不显示真实总源数时，不自行填写全清率100%。`upload_confirmed`仅在确认上传成功后写`true`。

填写`formal_results.csv`后运行`python paper/build.py`，正式表会同步更新；正文摘要、结论和分析需要按真实结果人工补写。敏感性、消融及问题二实验尚未完成，可以实测后填入，也可以据实删去空表并调整为未开展的后续工作，不能伪造数值。

人工检查模型、代码、数据、引用及完整程序附录；编辑`paper/ai-usage.tex`中实际核验说明、使用工具和日期，不写姓名学校，用匿名代号。确认后再把`human_review.json`对应事项设为true。

官方日志的原文件名不能改，签名/加密内容不能编辑。打包会核对题号、正式次数、案例编码和CSV文件名；不会验证签名真伪或解密，也不会代替模拟器上传。原始日志不会推送到公开Git；新增正式日志后重新打包的ZIP也应仅用于比赛提交，勿公开发布其中的系统身份信息。

机器人本地`runs/`可能含队号，不要原样放入这些目录。需要辅助动作记录时，在副本中匿名化身份字段，保留原始记录用于核对；原始官方`.jlog`按上一段单独处理。
