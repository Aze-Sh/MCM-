结果应围绕题目所问组织，并用表格给出核心量、单位与不确定性。图~\ref{fig:workflow} 展示从数据到结论的最小闭环。
\begin{figure}[htbp]
  \centering
  \fbox{\parbox[c][36mm][c]{0.82\linewidth}{\centering
  数据审计 $\rightarrow$ 候选模型比较 $\rightarrow$ 留出检验\\[4pt]
  $\rightarrow$ 敏感性分析 $\rightarrow$ 可复现结论}}
  \caption{建模与验证闭环示意图}
  \label{fig:workflow}
\end{figure}

\begin{table}[htbp]
  \centering
  \caption{候选模型的留出集比较示例}
  \begin{tabular}{lccc}
    \toprule
    模型 & 误差指标 & 稳定性指标 & 是否通过诊断 \\
    \midrule
    基线模型 & 1.00 & 0.84 & 是 \\
    候选模型 & 0.82 & 0.91 & 是 \\
    \bottomrule
  \end{tabular}
\end{table}
