# B 题模拟器接入说明

依据用户提供的 `D:/QQ/ABC题.zip` 内 B 题附件 1、附件 2。原件完整归档在 `raw/B题附件/`，正文及数学文本提取在 `processed/附件1提取.txt`、`附件2提取.txt`。提取稿不能替代原件的表格和公式排版。

## 材料状态

- 附件 1《模拟器使用说明》：已读取，SHA-256 为 `20a27603ea81efa8f11658b3fa5b859a69aa3ff88fb3664d2abb80d740b47553`。
- 附件 2《模拟器通信接口说明及编程指南》：已读取，SHA-256 为 `c882513d5b7e0ec50f3068570ea55fdc1b5c4a5fa2d6e9e54e79b33cf0858cb2`。
- 压缩包没有模拟器可执行程序。附件 1 提供的下载地址为 https://pan.baidu.com/s/1P1yfVjY0RufU93XOdzhOLw?pwd=2026 ，提取码 `2026`；本次未访问或下载该链接。
- 模拟器为解压即用程序，数据保存在程序所在目录，迁移时应整体保留。首次注册需要队伍在本机填写报名队号、队员 1 姓名及手机号并设置密码；不要把密码写进代码或比赛论文。
- 用户随后提供下载目录 `C:/Users/37828/Desktop/b题/`，内有标准版、完整版、下载说明和操作演示视频。下载说明建议 Windows 10/11 优先标准版，缺运行环境时再用完整版。
- 标准版已解压到 `C:/Users/37828/Desktop/国赛/tmp/simulator-standard/Jammers-simulator/`，可执行文件 `jammers-simulator.exe`，SHA-256 为 `2373b9e7af83735a04309e2983eb433ec46faf7e0b8494410ce7fded2a297c27`。源下载文件未修改。
- 已确认模拟器可见界面、在线登录和本机 `127.0.0.1:2026` 接口。2026-09-11 首次问题 3 官方演练完整执行 `/enter`、704 次 `/measure`、11 次成功 `/clear` 和 `/exit`；717 条请求全部接受，无通信错误。
- 程序已经生成同级 `JammersSimulatorData/`。虽然运行目录位于 Git 忽略的 `tmp/` 下，比赛期间必须保留整个 `tmp/simulator-standard/`，不能随临时文件清理。需迁移时先正常退出模拟器，再整体复制程序及数据目录。

## 四条接口

默认地址 `http://127.0.0.1:2026`，仅本机访问；全部使用 POST，Content-Type 为 `application/json`，请求体为无 BOM 的 UTF-8 JSON。

共同字段：`arena_id="default"`、`robot_id=当前登录队号`、`request_id=该动作唯一标识`。`/measure`、`/clear` 另需 `position={"x":数值,"y":数值}` 与 `channel=1..20`。

| 接口 | 作用 | 接受后状态/时间变化 |
|---|---|---|
| `/enter` | 开始运行 | 初始位置 (0,0)、频道 1；不增加虚拟时间；读取 `remaining_real_duration_s` |
| `/measure` | 移动并检测目标频道 | 路程/5 + 切频 0 或 1 + 检测 5 秒；更新位置及测向机频道 |
| `/clear` | 移动并尝试清除指定频道源 | 路程/5 + 失败 3 秒或成功 5 秒；更新位置，不改变测向机频道 |
| `/exit` | 主动结束 | 不增加虚拟时间；成功返回 `exit_reason="user_exit"` |

`/measure` 的 `measure_result` 为 `direction`、`near`、`no_signal`。仅 `direction` 有 `svd_deg`；`near` 表示辐射范围内且距离不超过 5 m，可在当前位置尝试清除。`/clear` 的 `clear_result` 为 `success` 或 `no_target_in_range`。

## 实现约束

1. 逐次收到完整 HTTP 响应后再发新动作，不并发发送动作；每次检测无需在现实中等待 5 秒。
2. 网络超时或连接中断时，对未确认动作只重试完全相同的路径、正文、request_id。相同 ID 与内容返回原响应，不重复计时。不同动作使用新 ID；同一 ID 改内容会触发 409。
3. 同时检查 HTTP 状态和 `accepted`；HTTP 200 不保证执行成功。`accepted=false` 的 `virtual_time_s=0` 不能用来重置本地状态，应保留最近一次已接受状态。
4. 不添加未声明字段，包括 position 内的额外字段。坐标必须有限、分量绝对值≤2000000；频道为 1—20 的整数。
5. 使用 `/enter` 返回的实际剩余时间控制截止并留出退出余量；不能固定假设有 1200 秒。测试结束后接口关闭，不调用 `/exit` 查询结束原因。
6. 仅在已确认接受后更新位置、频道、累计清除数和时间；同一动作重试返回的旧响应不能二次计入清除数。
7. 示向度保留两位小数。理论题按 ±1° 分析；工程端可为量化误差增加保守裕量，但需明确这属于实现假设，不能宣称附件规定为 ±1.005°。
8. 保存本地动作及响应日志；模拟器的加密正式日志仍须另外原名导出。日志中的队号属于本地接入信息，提交前需要单独按规则核对匿名要求，不能修改官方要求保留的原始加密日志。

## 对账与首次演练

附件计时示例：进入 → 在 (300,400) 测频道 1 → 原地测频道 2 → 到 (300,0) 清除频道 3（假设失败）→ 原地测频道 2 → 退出。累计虚拟时间依次为 0、105、111、194、199、199 秒。清除不改变测向机频道是这一例子的关键。

接入程序先在本地模拟响应中验证计时、三类检测、两类清除、拒绝响应、同 ID 重试与超时；再在官方演练中验证真实协议及策略。附件示例程序只演示通信，不是完整搜索算法，不直接用于正式测试。

首次官方问题 3 演练案例为 `AFYH-6GTT-GWWB-ZDPW`。结果页及模拟器生成的 `result.json` 确认共 11 个源（全向 11、定向 0），程序清除 11 个，清除比例 1.0；虚拟总时间 16929.531418 秒，平均定位清除时间 1539.048311 秒，现实运行 6.209766 秒。本地动作日志为 `logs/practice-20260911-085512-bfd14d.jsonl`，大小 236703 字节。协议正常路径与本地时间账差异约 0.000002 秒。模拟器生成的三份演练行为文件已按原名备份至 `logs/official-practice/q3-first/`。

第二次问题 3 官方演练使用 v2，案例 `FYKN-VY2Y-C99F-6BZN`，共 15 个全向源并全部清除；虚拟总时间 16696.588548 秒，平均定位清除时间 1113.105903 秒，现实运行 4.367882 秒。515 条请求全部接受，无通信错误；模拟器原始文件保存在 `logs/official-practice/q3-second-v2/`。

第三次问题 3 官方演练使用 v3，案例 `8BS5-GQWW-6BTJ-8Z73`，共 15 个全向源并全部清除；虚拟总时间16282.901650秒，平均定位清除时间1085.526777秒，现实运行4.432264秒。516条请求全部接受，没有失败清除和通信错误；模拟器原始文件保存在 `logs/official-practice/q3-third-v3/`。

首次问题 4 官方演练使用 v2，案例 `77QS-523V-UX4E-UCGC`，共 14 个源（全向 11、定向 3）并全部清除；虚拟总时间 15927.931031 秒，平均定位清除时间 1137.709359 秒，现实运行 5.301661 秒。583 条请求全部接受，无通信错误；模拟器原始文件保存在 `logs/official-practice/q4-first-v2/`。

第二次问题 4 官方演练案例 `UC47-56D3-EHEC-RTK3`，共 16 个源（全向 7、定向 9）并全部清除；虚拟总时间 17598.383071 秒，平均定位清除时间 1099.898942 秒，现实运行 5.406442 秒。525 条请求全部接受，没有失败清除和通信错误；模拟器原始文件保存在 `logs/official-practice/q4-second-v2/`。

第三次问题 4 官方演练使用 v3，案例 `F6QC-7NFR-B6ES-WJMF`，共 11 个源（全向 5、定向 6）并全部清除；虚拟总时间 16538.538737 秒，平均定位清除时间 1503.503522 秒，现实运行 6.644723 秒。749 条请求全部接受，没有失败清除和通信错误；模拟器原始文件保存在 `logs/official-practice/q4-third-v3/`。

模拟器运行后由队伍完成登录并选择“问题 3 演练测试”或“问题 4 演练测试”；接口只在测试窗口内开放，空闲时连不上不代表安装损坏。正式测试启动成功即消耗机会，本次材料接收不构成启动正式测试的授权。
